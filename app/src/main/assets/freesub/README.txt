Place free config files here (one per file):
- *.conf   → AmneziaWG (with Jc/Jmin/Jmax/S1/S2/H1-H4)
- awg://   or anytls:// or vless:// links (one per line)
Files are bundled into the app as the locked "Free" group.
Users cannot view/edit/share/delete them — only ping and connect.
Skip non-config files: use extension .conf or .txt only.
